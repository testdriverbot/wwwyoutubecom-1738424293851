version: 4.1.58
session: 679e41aabb803650f38edddf
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: >-
      Assert that the "Sign in" button is visible and clickable.  # Use the
      content of the file from the environment
    commands:
      - command: hover-text
        text: Sign in
        description: sign in button on the YouTube page
        action: click
      - command: assert
        expect: the "Sign in" button is visible and clickable
